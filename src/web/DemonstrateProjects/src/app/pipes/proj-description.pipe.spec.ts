import { ProjDescriptionPipe } from './proj-description.pipe';

describe('ProjDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
